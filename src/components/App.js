import React, { useReducer } from 'react';

const App = () => {
 <>
 <Profile 
 avatar={user.avatar}
 name={user.name}
 tag={user.tag}
 location={user.location}
 stats={user.stats}>
 </>
}

export default App;
